<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <group android:id="@+id/main_menu_group">

        <item
            android:id="@+id/action_show_screen_controls"
            android:title="@string/action_show_screen_controls"
            android:orderInCategory="0"
            app:showAsAction="never"
            />

        <item
            android:id="@+id/action_reset_screen_controls"
            android:orderInCategory="1"
            android:title="@string/action_reset_screen_controls"
            app:showAsAction="never"
            />

        <item
            android:id="@+id/action_reset_config"
            android:orderInCategory="2"
            android:title="@string/action_reset_config"
            app:showAsAction="never"
            />

    </group>
</menu>